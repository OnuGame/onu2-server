import { CardPreset } from "../CardPreset";
import { CLASSIC_PRESET } from "./ClassicPreset";

export const ACTION_PRESET: CardPreset = {
    colors: CLASSIC_PRESET.colors,
    types: ["p2", "sk", "sw"],
};
